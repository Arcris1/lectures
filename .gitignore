# ── Private: infrastructure notes for vps-prod-01 ─────────────────────────────
# These contain the server's live configuration, a home IP address, and key
# identifiers. They are deliberately NOT in this repository, which means a
# build run from a clone physically cannot include them.
vps-*.md
vps-*.sh

# ── Build output ──────────────────────────────────────────────────────────────
# Generated by `node build-viewer.mjs`. Rebuilt on deploy, so it is not tracked
# (and the local build contains the private docs above).
index.html

# ── Misc ──────────────────────────────────────────────────────────────────────
*.zip
.DS_Store
Thumbs.db
node_modules/
